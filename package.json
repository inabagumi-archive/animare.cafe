{
  "dependencies": {},
  "description": "The animare.cafe is a unofficial website",
  "devDependencies": {
    "@inabagumi/prettier-config": "^1.0.0",
    "prettier": "^1.18.2"
  },
  "license": "MIT",
  "name": "@inabagumi/animare.cafe",
  "prettier": "@inabagumi/prettier-config",
  "private": true,
  "scripts": {
    "format": "prettier --ignore-path .gitignore --write \"**/*.{json,md,yml}\"",
    "lint": "prettier --ignore-path .gitignore --check \"**/*.{json,md,yml}\""
  },
  "version": "5.0.0"
}
